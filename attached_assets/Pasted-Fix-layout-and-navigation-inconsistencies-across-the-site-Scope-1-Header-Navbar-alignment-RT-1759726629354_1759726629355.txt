Fix layout and navigation inconsistencies across the site.

Scope:
1. **Header/Navbar alignment & RTL**
   - Ensure Navbar renders only once (inside Layout component, not in each page).
   - Fix direction logic: `<nav dir={i18n.language === 'ar' ? 'rtl' : 'ltr'}>`.
   - Ensure all layout spacing (padding/margin) uses logical CSS properties (`padding-inline`, `margin-inline-start`, etc.) instead of left/right.
   - In index.css or global.css add:
     html[dir="rtl"] body { direction: rtl; text-align: right; }
     html[dir="ltr"] body { direction: ltr; text-align: left; }
   - Make sure the logo and navigation buttons stay horizontally centered and visually balanced in both AR and EN.

2. **Footer links not working**
   - Replace any `<a href="#">` or `<a>` tags with `<Link to="...">` from react-router-dom.
   - Ensure Footer links point to correct routes:
       /new  → New Perfumes
       /used → Used Perfumes
       /auctions → Auctions
       /help → Help or Policies page
   - Add hover styles consistent with the gold theme.

3. **Router consistency**
   - Confirm BrowserRouter wraps the app once (in main.tsx or App.tsx).
   - Verify all routes exist for `/new`, `/used`, `/auctions`, `/help`, `/account`, `/seller`, `/admin`.
   - Ensure navigation works client-side (no full reloads).

4. **Visual balance**
   - Adjust header height and logo alignment for better centering.
   - Use consistent spacing tokens (8,16,24,32px) for nav and footer padding.
   - Ensure header and footer colors match brand palette (Charcoal background, Gold hover).

5. **Testing**
   - Test switching AR/EN: header, footer, and direction update instantly.
   - Test all footer buttons navigate correctly without page reload.
   - Fix any remaining console warnings related to missing `key` or `Link` components.

Constraints:
- Keep all fixes within layout, header, footer, and routing files only.
- Do NOT modify logic for API or dashboards.
- Keep output minimal; show changed file paths only and create a checkpoint when done.