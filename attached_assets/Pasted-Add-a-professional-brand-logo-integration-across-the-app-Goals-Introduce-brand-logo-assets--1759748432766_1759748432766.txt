Add a professional brand logo integration across the app.

Goals:
- Introduce brand logo assets (monogram + EN wordmark + AR wordmark + bilingual lockup).
- Wire the logo into the Header/Navbar and Footer with proper sizing, RTL/LTR behavior, and dark mode variants.
- Add favicon + PWA manifest icons and update index.html.

Tasks:

1) Assets
- Create folder: frontend/src/assets/brand/
- Add placeholders (SVG as source of truth) named:
  - logo-ao-monogram.svg
  - logo-ao-wordmark-en.svg
  - logo-ao-wordmark-ar.svg
  - logo-ao-lockup-bilingual.svg
- Export crisp PNGs from SVG (generated or placeholders):
  - monogram: 512, 192, 180, 128, 64, 32
  - wordmarks: 1024, 512, 256
  - lockup-bilingual: 1200x630 (OG), 1080x1080 (social)
- Add favicon set under frontend/src/assets/brand/favicon/:
  - favicon.ico (16,32,48)
  - favicon-32.png, favicon-180.png, favicon-192.png, favicon-512.png

2) BrandLogo component
- Create frontend/src/components/brand/BrandLogo.tsx with props:
  - variant: "monogram" | "wordmark-en" | "wordmark-ar" | "lockup-bilingual"
  - theme: "light" | "dark"
  - size?: number (px)
  - className?: string
- Behavior:
  - Auto-pick wordmark-en or wordmark-ar based on i18n language if variant="wordmark-*".
  - Provide ARIA labels localized via i18n (e.g., "Aalam Al-Otoor logo" / "شعار عالم العطور").
  - If theme="dark", use white/ivory wordmark fallback if available; else keep gold monogram on dark backgrounds.
- Ensure the component returns an <img> with width/height based on size and loading="lazy".

3) Header/Navbar wiring
- Replace any text logo with <BrandLogo />:
  - Desktop: variant="lockup-bilingual", size ~160
  - Mobile: variant="monogram", size ~28–32
- Maintain clickable link to home ("/") with <Link>.
- Preserve spacing tokens and RTL/LTR alignment; logo sits flush with nav elements via logical CSS (margin-inline).

4) Footer wiring
- Use <BrandLogo variant="wordmark" ...> centered on mobile, left-aligned on desktop.
- Ensure links remain <Link to="..."> and do not trigger full reload.

5) PWA & Favicon
- Create/Update frontend/public/manifest.webmanifest with:
  - name, short_name, theme_color "#C8A24A", background_color "#F7F5F2", display "standalone"
  - icons: point to 192/512 png in brand/favicon
- Update index.html:
  - <link rel="icon" ...> for favicon.ico and 32.png
  - <link rel="apple-touch-icon" href="/assets/brand/favicon/favicon-180.png">
  - <link rel="manifest" href="/manifest.webmanifest">
  - Preload Inter and Cairo fonts if not already.

6) Dark Mode support
- When body has dark theme, ensure logo remains legible:
  - Prefer gold monogram + ivory/white wordmark on dark backgrounds.
  - If needed, add CSS utility `.brand-on-dark` to force suitable variant.

7) Acceptance tests
- Header shows bilingual lockup on desktop, monogram on mobile.
- RTL/LTR switch updates ARIA label and keeps alignment correct.
- Footer logo visible and links work client-side.
- Favicon appears in tab; Lighthouse PWA icons pass.
- No layout shift or overflow in header/footer.

Constraints:
- Keep output minimal. Do not alter API logic.
- Print CHANGED PATHS ONLY and create ONE checkpoint when done.
