PORT=8080
NODE_ENV=development

# Prisma / MySQL connection string format:
# mysql://USER:PASSWORD@HOST:PORT/DATABASE?connection_limit=10
DATABASE_URL=mysql://user:password@localhost:3306/otour_marketplace

# JWT configuration (adjust as needed)
JWT_SECRET=change_me
JWT_EXPIRES_IN=86400

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5000

# Google OAuth
GOOGLE_CLIENT_ID=__REPLACE_WITH_GOOGLE_CLIENT_ID__
GOOGLE_CLIENT_SECRET=__REPLACE_WITH_GOOGLE_CLIENT_SECRET__

# Mail / Reset Password
MAIL_HOST=live.smtp.mailtrap.io
MAIL_PORT=587
MAIL_USERNAME=api
MAIL_PASSWORD=__REPLACE_WITH_MAILTRAP_PASSWORD__
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=no-reply@souftech.com
MAIL_FROM_NAME="Doctors Directory"
PASSWORD_RESET_URL=https://fragraworld.com/reset-password
